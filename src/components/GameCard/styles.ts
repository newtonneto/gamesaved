import styled from 'styled-components/native';
import { Star, FloppyDisk } from 'phosphor-react-native';

export const StarStyled = styled(Star)`
  margin-right: 4px;
`;

export const FloppyDiskStyled = styled(FloppyDisk)`
  margin-right: 4px;
`;
